from app.config import settings

print("CLIENT ID REPR:", repr(settings.google_client_id))
print("CLIENT SECRET REPR:", repr(settings.google_client_secret))
print("REDIRECT URI REPR:", repr(settings.google_redirect_uri))
